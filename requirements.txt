# UI (>=1.30: client.showSidebarNavigation in config.toml)
streamlit>=1.30.0
streamlit-mic-recorder

# AI / speech APIs
google-genai
google-cloud-texttospeech
gTTS

# Data & visualization
pandas
plotly
matplotlib

# PDF export (final report download — install on Render with main app deps)
pillow>=10.0.0
reportlab>=4.0.0
