# UI (>=1.30: client.showSidebarNavigation in config.toml)
streamlit>=1.30.0
streamlit-mic-recorder

# AI / speech APIs
google-genai
google-cloud-texttospeech
gTTS

# Data & visualization
pandas
plotly
matplotlib

# PDF export (optional runtime; app runs without these — see services/pdf_report.py)
pillow
reportlab
